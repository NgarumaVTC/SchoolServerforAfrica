#+TITLE: Ngaruma School Network Architecture
#+AUTHOR: Axel
#+DATE: 2026-04-30
#+OPTIONS: toc:2

* Overview and Core Principles
This document outlines the network architecture for the Ngaruma School deployment in Tanzania.

** Core Principles applied:
- *Keep it simple, stupid! (KISS)*: IPv6 is completely disabled to reduce overhead and troubleshooting complexity. Security relies on simple, robust Layer-2 isolation and firewall rules rather than complex routing.
- *High-Performance Layer-2 Design*: To prevent routing bottlenecks, all heavy internal traffic (PXE booting 20 Thin Clients, NFS mounts, RDP sessions to NixOS) is kept locally on the main switch in the new building. The router only handles internet traffic and DNS.
- *Physical Resilience*: The ~400m connection between the main building (Router) and the new building (NixOS/Thin Clients) will be established using a fiber optic link (SFP+) to ensure complete galvanic isolation and immunity against lightning strikes.
- *Declarative Single Point of Truth (SPoT)*: All network configurations (IPs, VLANs, MAC addresses) are centrally defined in the NixOS configuration (`gitNixos/common/network.nix`).

* IP Addressing & Subnet Architecture
The network is divided into four distinct logical zones. VLANs are only used where technically necessary to transport multiple networks over a single trunk cable.

| Subnet         | Name            | VLAN ID | Interface / Port            | Purpose & Access Status                                  |
|----------------+-----------------+---------+-----------------------------+--------------------------------------------------|
| 172.20.0.0/24  | Admin & Mgmt    | 1 (unt) | Bridge / ether2-4 / wifi1   | Core management, NixOS Host, Full access.        |
| 172.20.89.0/24 | Teachers        | 89      | vlan-teachers / wifi-teach  | Teacher PCs and Teacher Wi-Fi.                   |
| 172.20.90.0/24 | Students Wired  | 90      | vlan-students               | Thin Clients booting via PXE from NixOS.         |
| 172.20.91.0/24 | Students Wi-Fi  | None    | wifi2 (Isolated)            | Isolated 2.4GHz Wi-Fi for student devices.       |

* Hardware Layout & Port Assignment

** 1. Router: MikroTik hAP ax3 (Main Building)
Acts as the central gateway, DHCP server, and firewall.

- *ether5*: WAN Uplink (DHCP Client from cellular modem).
- *ether1*: Trunk to Switch (VLAN 1 Untagged, VLAN 89 Tagged, VLAN 90 Tagged). *Note: PoE-out is strictly disabled here.*
- *ether2 - ether4*: Local Admin/Management access (Untagged, 172.20.0.x).
- *wifi1 (5GHz)*: SSID = =NgarumaAdmin=. Core management Wi-Fi.
- *wifi-teachers (5GHz Virtual)*: SSID = =NgarumaTeachers=. Mapped to VLAN 89.
- *wifi2 (2.4GHz)*: SSID = =NgarumaStudents=. Hard-removed from the bridge for Layer-2 isolation. 20MHz channel width for maximum penetration through clay walls.

** 2. Main Switch: CSS610 / CRS (New Building)
Acts as the distribution layer for the NixOS server and Thin Clients. Configured via SwOS (Strict VLAN Mode).

| Port | Connection Target   | VLAN Mode | VLAN Receive  | Default VLAN ID | Description                                      |
|------+---------------------+-----------+---------------+-----------------+--------------------------------------------------|
| 1    | Uplink to hAP ax3   | strict    | any           | 1               | Trunk port receiving tagged and untagged traffic |
| 2    | Management/Admin PC | strict    | only untagged | 1               | Local port for switch/server management          |
| 3    | NixOS Server        | strict    | any           | 1               | Trunk for NixOS (Host=Untagged, Containers=Tag)  |
| 4-8  | Thin Clients        | strict    | only untagged | 90              | Access ports for student Thin Clients            |

/Note: The CSS610 serves as a temporary simulation for the planned CRS 24-Port Switch. The logic remains identical./

* Security & Infrastructure Services

** DNS Filtering
All networks are forced to use Cloudflare Family DNS (=1.1.1.3=, =1.0.0.3=) for baseline malware and adult-content filtering.

** Access Control & Isolation
- *SSH*: Password authentication is completely disabled. Ed25519 public key authentication is strictly enforced. Strong-crypto is enabled.
- *Student Wi-Fi Isolation*: A firewall forward-chain drop rule prevents any traffic originating from =172.20.91.0/24= from reaching the local LANs (=172.20.0.0/24=, =.89.0/24=, =.90.0/24=). Students only have internet access. Client isolation (station-roaming=no) is active.

** System Settings
- *Timezone*: Africa/Dar_es_Salaam (EAT).
- *NTP*: Synced against =0.pool.ntp.org= and =1.pool.ntp.org=. Router acts as local broadcast/multicast NTP server for clients.
- *Hardware LEDs*: Controlled via script bound to the physical mode-button ("dark-mode").

* Operational Notes & Maintenance
- *Emergency Access*: If locked out via VLANs, connect a laptop to =ether2= on the hAP ax3. It defaults to the native =172.20.0.x= management subnet.
- *Adding a new Teacher Wi-Fi*: Can be done by creating a new Virtual AP bound to =wifi1= and setting =datapath.vlan-id=89=.
- *NixOS Container Networking*: Networking is completely declarative. When adding a new container, it is defined in =gitNixos/common/network.nix= with a statically generated MAC address and assigned to a VLAN (e.g., VLAN 90 for student services). NixOS will automatically configure the required virtual interfaces and bridges on the host upon rebuilding.

* Server & Container IP Allocation

To maintain strict control and simplify administration, the network configuration is centrally managed via `gitNixos/common/network.nix`. This file acts as the Single Point of Truth (SPoT) for all IP addresses, VLANs, and MAC addresses. 

The `nixos-containers` use generated, predictable MAC addresses mapped to static IPs within this configuration. Physical Thin Clients have their burned-in hardware MAC addresses registered in the same file to receive static DHCP leases (`sLease`) from the MikroTik router.

** 1. Infrastructure Core (Untagged / Management)
These services operate on the native VLAN 1 and only require access from the Admin network.

| Hostname      | IP Address    | Type   | Network / VLAN | Purpose                                    |
|---------------+---------------+--------+----------------+--------------------------------------------|
| hapax3        | 172.20.0.1    | Static | Admin (Untag)  | Core Router & DHCP Server                  |
| ngarumavtc1   | 172.20.0.10   | Static | Admin (Untag)  | NixOS Base Server / Host                   |
| mgmt1         | 172.20.0.11   | Static | Admin (Untag)  | Ansible, Scripts, etckeeper (Container)    |
| web01         | 172.20.0.21   | Static | Admin (Untag)  | Web Server / iPXE Boot Images (Container)  |

** 2. Shared Services (Multi-Homed Containers)
Declaratively configured nixos-containers can receive multiple virtual network interfaces mapped to different bridges/VLANs (e.g., eth0=untagged, eth1=tag 89, eth2=tag 90). Clients connect to the IP in their respective subnet to keep traffic strictly on Layer 2.

| Hostname      | IP Addresses                                       | Type   | Purpose                                            |
|---------------+----------------------------------------------------+--------+----------------------------------------------------|
| storage01     | .0.20 (Admin), .89.20 (Teachers), .90.20 (Students)| Static | ZFS NFS Exports (Homes, Lehrpult, Briefkasten)     |
| kiwix01       | .0.22 (Admin), .89.22 (Teachers), .90.22 (Students)| Static | Offline Wikipedia / Khan Academy                   |

** 3. Student Environments & Endpoints
| Hostname      | IP Address (Range)     | Type          | Network / VLAN | Purpose                                    |
|---------------+------------------------+---------------+----------------+--------------------------------------------|
| ctauth        | 172.20.90.12           | Static        | Students (90)  | Auth / Directory Services (Container)      |
| ct90051 - 70  | 172.20.90.51 - .70     | Static        | Students (90)  | 20x XFCE4 Workspaces (Containers for RDP)  |
| tc90151 - 170 | 172.20.90.151 - .170   | DHCP sLease   | Students (90)  | Physical Business PCs (iPXE Boot -> RDP)   |
| tc01          | 172.20.90.100          | DHCP sLease   | Students (90)  | Example Endpoint Registration              |
| teacher-pcs   | 172.20.89.100 - .254   | DHCP Dynamic  | Teachers (89)  | Teacher BYOD (DHCP Pool)                   |
